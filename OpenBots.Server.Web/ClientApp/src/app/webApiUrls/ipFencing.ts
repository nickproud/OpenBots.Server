export enum IpFencingApiUrl {
  organizations = 'organizations',
  IPFencing = 'IPFencing',
  organizationSettings = 'organizationSettings',
  mode = 'mode',
  denyAll = 'denyAll',
  allowAll = 'allowAll',
}
